# Upstream has a workflow for testing with thread sanitization. See
# https://github.com/ocaml/ocaml/blob/master/.github/workflows/tsan.yml.
name: tsan
on:
  workflow_dispatch:  # Manual trigger only
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TSan workflow not yet implemented for OxCaml"
          exit 1
